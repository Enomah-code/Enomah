# ============================================================
#  Angeleck OS — dépendances backend
# ============================================================

# --- API / Serveur ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.9.0
pydantic-settings>=2.6.0
email-validator>=2.2.0

# --- Architecture IA / Agents ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0

# --- Modèles locaux (Ollama) accès HTTP ---
httpx>=0.27.0

# --- Base de données PostgreSQL ---
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
psycopg2-binary>=2.9.10        # accès synchrone (Streamlit / Alembic)

# --- Mémoire vectorielle ---
chromadb>=0.5.0
sentence-transformers>=3.3.0

# --- Authentification / Sécurité ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- Outils agents (web, fichiers, data) ---
duckduckgo-search>=6.3.0
beautifulsoup4>=4.12.0
pandas>=2.2.0
openpyxl>=3.1.5               # lecture .xlsx
numpy>=1.26.0

# --- Interface admin ---
streamlit>=1.40.0
requests>=2.32.0

# --- Divers ---
python-dotenv>=1.0.0
loguru>=0.7.0
