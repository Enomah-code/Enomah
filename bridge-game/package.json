{
  "name": "bridge-architect",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Jeu de stratégie d'architecte de ponts — construisez, testez, survivez à des charges toujours plus lourdes.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "build:capacitor": "tsc --noEmit && cross-env CAPACITOR_BUILD=1 vite build",
    "cap:sync": "npm run build:capacitor && npx cap sync",
    "android:build-debug": "npm run cap:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2",
    "matter-js": "^0.20.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.5.2",
    "@types/matter-js": "^0.19.7",
    "cross-env": "^10.1.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
